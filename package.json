{
  "name": "aori-room-cloudflare-deploy",
  "private": true,
  "scripts": {
    "build": "rm -rf app && mkdir app && unzip -q aori-room-source.zip -d app && node fix-apply-v1.1.mjs && node apply-v1.1.mjs app && node apply-v1.2.mjs app && cd app && rm -f package-lock.json && bun install && bun run test && bun run build",
    "deploy": "cd app && bunx wrangler deploy"
  }
}
