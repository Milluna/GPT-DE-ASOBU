{
  "name": "aori-room-cloudflare-deploy",
  "private": true,
  "scripts": {
    "build": "rm -rf app && mkdir app && unzip -q aori-room-source.zip -d app && unzip -qo aori-room-overrides-v1.2.zip -d app && cd app && rm -f package-lock.json && bun install && bun run test && bun run build",
    "deploy": "cd app && bunx wrangler deploy"
  }
}
