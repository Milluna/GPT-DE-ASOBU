{
  "name": "gpt-de-asobu",
  "version": "1.3.0",
  "scripts": {
    "test": "vitest run",
    "dev": "vite --host 0.0.0.0",
    "dev:worker": "wrangler dev --local --port 8787",
    "dev:full": "npm run build && npm run dev:worker",
    "build": "npm run test && npm run build:app",
    "deploy": "bunx wrangler deploy",
    "preview": "vite preview --host 0.0.0.0",
    "smoke:network": "node scripts/network-smoke.mjs",
    "check": "npm run test && npm run build",
    "build:app": "tsc -b --pretty false && vite build"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "description": "Two-person mobile 3D communication room prototype for iPhone Safari.",
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260712.1",
    "@types/node": "26.1.1",
    "@types/three": "0.185.1",
    "typescript": "7.0.2",
    "vite": "8.1.4",
    "vitest": "4.1.10",
    "wrangler": "4.110.0"
  },
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "bun@1.2.15"
}
